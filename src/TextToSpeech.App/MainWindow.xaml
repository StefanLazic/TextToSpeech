<Window x:Class="TextToSpeech.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Tekst u govor (Srpski) — Azure AI Speech"
        Height="480" Width="720"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Unesite tekst na srpskom jeziku:"
                   FontWeight="SemiBold"
                   Margin="0,0,0,6" />

        <TextBox x:Name="InputTextBox"
                 Grid.Row="1"
                 AcceptsReturn="True"
                 TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Auto"
                 FontSize="14"
                 Text="Здраво! Ово је демонстрација претварања текста у говор." />

        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,12,0,0">
            <TextBlock Text="Глас:" VerticalAlignment="Center" Margin="0,0,8,0" />
            <ComboBox x:Name="VoiceComboBox" Width="240" />
        </StackPanel>

        <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,12,0,0">
            <Button x:Name="SpeakButton"
                    Content="🔊 Изговори"
                    Padding="14,6"
                    Click="SpeakButton_Click" />
            <Button x:Name="SaveButton"
                    Content="💾 Сачувај WAV…"
                    Padding="14,6"
                    Margin="8,0,0,0"
                    Click="SaveButton_Click" />
        </StackPanel>

        <TextBlock x:Name="StatusTextBlock"
                   Grid.Row="4"
                   Margin="0,12,0,0"
                   Foreground="#444"
                   TextWrapping="Wrap" />
    </Grid>
</Window>
